{
  "name": "george-b-johnson-static",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "devDependencies": {}
}
